[pytest]
# Kolorowanie wynikow testu
addopts = --color=yes
# Ustawia zakres petli zdarzen explicite na funkcje
asyncio_default_fixture_loop_scope = function
# Wyśewietla logi w trakie testu
adopts = -s -v